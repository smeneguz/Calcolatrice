#ifndef SELECT_H
#define SELECT_H
#include <QGridLayout>
#include <QLabel>
#include <QPushButton>
#include <QListWidget>
#include <QWidget>

//Select Zone
class Select : public QWidget {
    Q_OBJECT
public:
    Select(QWidget *parent = 0);

signals:

public slots:
};

#endif // SELECT_H
